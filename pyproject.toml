[project]
name = "sec-intelligence-mcp"
version = "0.1.0"
description = "MCP server for SEC EDGAR filing intelligence (search, retrieval, evaluation)"
authors = [
    { name = "jahanvi", email = "jahanvikpanchal01@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0,<2.0",
    "python-dotenv>=1.0",
    "qdrant-client>=1.19.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.6",
]
